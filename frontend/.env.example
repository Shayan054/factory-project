# Copy to .env.local for local overrides (optional).
# Dev: defaults come from .env.development
# Production/hosting: set VITE_API_URL in the host dashboard (do not commit secrets).
VITE_API_URL=http://127.0.0.1:8000/api
